


class Robot {
public:
    void move_forward(const &double distance);
    void turn_right(const double& angle);
    void turn_left(const double& angle);
    void compute_best_path();
    void solve_maze();
}